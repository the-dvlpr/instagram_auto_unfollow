## Auto Unfollow for Instagram

Install the gems used in the script

```
$ gem install 'watir'
$ gem install 'colorize'
```

Run script
```
$ ruby auto_unfollow.rb
```

Enter username and password when prompted.

That's it.
